// API Configuration
const config = {
  API_BASE_URL: "http://localhost:5000/api",
  ENDPOINTS: {
    AUTH: {
      LOGIN: "/auth/login",
      REGISTER: "/auth/register",
    },
  },
};

export default config;
